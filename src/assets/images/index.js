/**
 * Images: declare and export all image assets from src/assets/images.
 * Add image files to this folder and reference them here so the app uses a single source.
 */

import tahwulLogo from './tahwul-logo.png';

export { tahwulLogo };
